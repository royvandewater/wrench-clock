# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: mysql2
  database: wrench_clock_development
  encoding: utf8
  username: root
  password:

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  database: wrench_clock_test
  encoding: utf8
  username: root
  password:

guard:
  adapter: mysql2
  database: wrench_clock_guard
  encoding: utf8
  username: root
  password:

# production:
#   adapter: mysql2
#   database: wrench_clock_production
#   encoding: utf8
#   username: root
#   password:
